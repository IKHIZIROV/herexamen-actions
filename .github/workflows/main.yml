# volgens mij werkt het niet omdat ik perongeluk toch 2FA heb gezet op mijn nieuwe account
# en heb geen tijd om een nieuwe te maken :(
on:
  push:
    branches:
    - main
jobs:
  sendmail:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: send email
      uses: devellany/send-mail@v1.0.2
      with:
        host: smtp.google.com
        account: IKHIZIROV
        password: ${{secrets.PASSWORD}}
        sender: ismail
        from: ${{secrets.ACCOUNT}}
        to: u8783806587@gmail.com
        subject: Aantal lijnen code
        body: Check de bijlage :)
        attachments: '[{"path":"linesofcode.txt"}]'
